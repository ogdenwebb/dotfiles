#!/usr/bin/env sh

emacsclient -a "" -n -c "$@"

# with dashboard
# if [ -n "$1" ]; then
#     emacsclient -a "" -n -c "$@"
# else
#     emacsclient -a "" -n -c -e "(switch-to-buffer \"*dashboard*\")"
# fi
